import React from 'react';
import ReactDOM from 'react-dom';

// Component file
import TodoContainer from './components/TodoContainer';

const element = <h1>First h1 element</h1>;

ReactDOM.render(<TodoContainer />, document.getElementById('root'));
